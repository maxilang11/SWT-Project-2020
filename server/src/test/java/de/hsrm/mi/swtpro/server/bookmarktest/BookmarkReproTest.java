//package de.hsrm.mi.swtpro.server.bookmarktest;

//import static org.assertj.core.api.Assertions.assertThat;

//import java.util.ArrayList;
//import java.util.Arrays;
//import java.util.List;

//import org.junit.jupiter.api.DisplayName;
//import org.junit.jupiter.api.Test;
//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.boot.test.context.SpringBootTest;
//import de.hsrm.mi.swtpro.server.models.Product;
//import de.hsrm.mi.swtpro.server.models.ShoppingCart;
//import de.hsrm.mi.swtpro.server.repository.ShoppingCartRepository;


//@SpringBootTest
//public class BookmarkReproTest {
    //private final int ID = 1;
    //Product P1 = new Product(1,"Bananen", 3, "Obst", 1.29, "Wiesbaden", "Germany", 1);
    //Product P2 = new Product(2,"Gurken", 4, "Gemüse", 2.29, "Mainz", "Deutschland", 2);
    //private final List<Product> PRODUCTLIST = new ArrayList<Product>(Arrays.asList(P1, P2));

    //@Autowired
    //private BookmarkRepository bookmarkRepro;

    //@Test
    //public void vorabcheck() {
    //assertThat(ShoppingCartRepository.class).isInterface();
    //assertThat(shoppingCartRepo).isNotNull();
     //}

    //@Test
    //@DisplayName("BookmarkRepro findByID")
    //public void bookmarkFindById() {
        //final int ANZAHL = 5;

     //bookmarkRepro.deleteAll();

       //for (int i=0; i < ANZAHL; i++) {
          //final Bookmark u1 = new Bookmark();
          //u1.setId(ID+i);
          //u1.setBookmarkList(PRODUCTLIST);
          //bookmarkRepro.save(u1);
       //}
       //assertThat(bookmarkRepro.count()).isEqualTo(ANZAHL);

       //for (int n=0; n < ANZAHL; n++) {
           //bookmarkRepro bookmark = bookmarkRepro.findById(ID+n);
           //assertThat(bookmark.getID() == ID+n).isTrue();
       //}
    //}


// }